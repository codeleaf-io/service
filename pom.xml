<project>

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.codeleaf.release</groupId>
        <artifactId>release</artifactId>
        <version>0.9.0</version>
    </parent>

    <groupId>io.codeleaf.service</groupId>
    <artifactId>service</artifactId>
    <version>0.9.0</version>

    <packaging>pom</packaging>

    <name>Service</name>
    <description>Library for running services</description>

    <properties>
        <camel.version>3.20.3</camel.version>
        <jaxrs.version>2.1.1</jaxrs.version>
        <jersey.version>2.39.1</jersey.version>
    </properties>

    <modules>
        <module>api</module>
        <module>tty</module>
        <module>jaxrs</module>
        <module>engines/tty</module>
        <module>engines/jaxrs</module>
    </modules>

    <developers>
        <developer>
            <name>Max</name>
            <email>rb33gh@opsmotor.com</email>
        </developer>
    </developers>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>svc-api</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>svc-tty</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>svc-jaxrs</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>svc-engine-tty</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>svc-engine-jaxrs</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

</project>
